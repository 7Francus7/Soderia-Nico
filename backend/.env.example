# ──────────────────────────────────────────
# Variables de entorno del Backend (FastAPI)
# ──────────────────────────────────────────
# Copiá este archivo como .env y completá los valores

# Base de datos — Railway te da esta URL automáticamente al crear PostgreSQL
# Formato: postgresql://user:password@host:port/dbname
DATABASE_URL=postgresql://user:password@host:5432/soderia_db

# Clave secreta para firmar JWT — Cambiá esto por algo aleatorio y largo
# Generá una con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CAMBIAR_ESTO_POR_UNA_CLAVE_SEGURA_DE_64_CARACTERES

# URL del frontend en Vercel (para CORS)
# Ej: https://soderia-nico.vercel.app
FRONTEND_URL=https://tu-app.vercel.app

# Tiempo de expiración del token JWT en minutos (8 días por defecto)
ACCESS_TOKEN_EXPIRE_MINUTES=11520
